let width = prompt('Enter the width a box:');
let height = prompt('Enter the height:');
let length = prompt('Enter the length:');

const volume = (width*height*length)

alert("The volumen of the box is: " + volume);

