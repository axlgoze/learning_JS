const names = [
    'John',
    'Axl',
    'Brandon',
    'Gina'
];

console.log(names.map(names=>names.length));

//Transform the array elements without changing the original array.

// creates a new array applying a function to each element.

// applies a function to each element creating a new array without changing the original array