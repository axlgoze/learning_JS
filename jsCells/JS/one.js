let names = "Carlos/José/Sandra/Ana/Rosa";

let aNames = names.split();

console.log(aNames.length);
// 1

// split() divides a string into a matrix of substrings.
// if there's no arg the output is only one element. in this case the entire string.