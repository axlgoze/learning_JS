const fruits = ['apple', 'pineapple', 'grape', 'watermelon'];
const { fruit1, fruit2 } = fruits;

console.log(fruit1);

// the destructure of arrays is with []
// the destructure of objects is with {}

/**
 * undefined
 */
