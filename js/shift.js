let cola = ['axel','Robert','Lily'];
// add an element at the end
cola.push('music')
// removes the first element, the 'cola' continues
cola.shift();

document.write(cola);

document.write('<hr>');

let pila = ['color','box','week'];
// add an element at the end
pila.push('radiation');
// take an element from the end
pila.pop();

document.write(pila)
